"""Cross-model coupling Interfaces for Flood-RM.

This package owns behavior that exists because two model domains meet. Wflow-only
logic stays in ``wflow_runs`` and SFINCS-only logic stays in ``sfincs_runs``.
"""

__all__: list[str] = []
