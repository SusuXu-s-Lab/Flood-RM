"""Baseline Network stage: SHIFT -> GDM -> DiTTo -> Asset Registry.

Produces the Synthetic Physical Network and the Asset Registry. Import the
concrete module, e.g. ``power.baseline_network.build_asset_registry``.
"""
