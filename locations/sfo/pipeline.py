"""Notebook order for the partial San Francisco location pipeline."""

GRID_NOTEBOOKS = (
    "01_grid/sds_plot.ipynb",
)

FLOOD_NOTEBOOKS = ()

PIPELINE_NOTEBOOKS = {
    "grid": GRID_NOTEBOOKS,
    "flood": FLOOD_NOTEBOOKS,
    "all": GRID_NOTEBOOKS,
}
