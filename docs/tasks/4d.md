/improve-codebase-architecture

You are the Wflow coupling and handoff refactor agent for Flood-RM.

Owned files:
- `src/wflow_runs/**`
- tests related specifically to `src/wflow_runs/**`
- `docs/agent_reports/wflow_runs.md`

Do not touch:
- `src/power/**`
- `src/design_events/**`
- `src/sfincs_runs/**`
- `src/fiat_runs/**`
- shared files outside `src/wflow_runs/**`, except TODO notes in your agent report
- notebooks, unless you only document the required future import migration

Read first:
- `AGENTS.md`
- `docs/plumbing_reduction_directive.md`
- `docs/codebase_map.md`
- `docs/notebook_backend_trace.md`
- `docs/refactor_roadmap.md`
- `docs/reduction_candidates.md`
- `docs/grill_review.md` if present
- `docs/agent_work_packages.md`
- `src/aoi.py`, `src/paths.py`, and `src/study_location.py` as production syntax examples

Goal:
Reduce `src/wflow_runs/**` toward direct scientific Python while preserving Wflow as the inland hydrologic generator and the Wflow-SFINCS handoff contract. Keep Wflow build planning, staticmap QA, calibration, streamflow realization, dynamic handoff, replay, state handling, and visualization understandable without generic wrappers.

Syntax standard:
- Prefer the style now used in `src/aoi.py`, `src/paths.py`, and `src/study_location.py`: small functions, direct `Path`, direct `pandas`/`xarray`/`geopandas`/`shapely`/`pyproj`, direct `json.dumps`/`yaml.safe_dump`, and dataclasses only for real plan/result records.
- Prefer native HydroMT-Wflow and xarray/geopandas operations over reimplementing table, raster, geometry, or NetCDF behavior.
- Do not move Wflow-specific handoff, replay, or staticmap semantics into `paths.py`, `aoi.py`, or `study_location.py`.

Deletion bridge:
1. Run `rg` call-site checks before deleting or moving anything.
2. Classify each candidate as `CORE_SCIENCE`, `NOTEBOOK_API`, `PLUMBING_IO`, `VALIDATION_QA`, `PLOTTING_REPORTING`, `REDUNDANT_OR_REPLACEABLE`, or `STALE_OR_DANGEROUS`.
3. Delete or inline only shallow modules/helpers that fail the deletion test.
4. Keep domain artifacts and schemas at the writer/reader that owns them.
5. Replace repeated path, JSON, manifest, scalar parsing, and dataframe wrappers with direct code when the behavior is ordinary library behavior.
6. Mark uncertain batch wrappers or competing implementations as "needs evidence"; do not delete them.

High-value targets:
- `build_plan.py`: reduce repeated path/source/staticmap planning code; split only if a module owns a coherent Wflow concept such as source strategy, HydroMT build execution, staticmap QA, or handoff planning.
- `replay.py`: keep meteorological forcing and event replay semantics explicit; use xarray/pandas directly for time-series transformations.
- `dynamic_handoff_batch.py` and coupled handoff modules: remove stale wrappers only after call-site evidence; preserve accepted/blocked event status semantics.
- `notebook.py`, visualization, and calibration helpers: remove notebook pass-throughs and plotting wrappers where direct imports or direct matplotlib/xarray calls are clearer.

Line-reduction expectations:
- Measure before/after line counts for touched files with `wc -l`.
- Prefer deletion or inlining over splitting into more files.
- A good pass should remove shallow code or materially shrink `build_plan.py`/`replay.py`; if a file remains large because it owns real Wflow coupling behavior, document why.

Do not change without explicit evidence and tests:
- Wflow/SFINCS handoff file formats, timestamps, units, routing semantics, baseflow/readiness gates, Wflow staticmap conventions, scenario folder conventions, event-window semantics, or the rule that inland discharge is a Wflow-derived response.
- Do not run Wflow.
- Do not run SFINCS.
- Do not download large datasets.
- Do not require credentials.

Run:
- `uv run python -m compileall src`
- `uv run python -m pytest tests/wflow_runs`
- `uv run python -m pytest`

Done when:
- Deleted/reduced helpers are listed with call-site evidence.
- Preserved Wflow and handoff APIs are listed.
- Remaining "needs evidence" candidates are documented.
- `docs/agent_reports/wflow_runs.md` records changed files, line-count reduction, validation results, and cross-domain TODOs.
