/improve-codebase-architecture

You are the SFINCS/SnapWave setup and scenarios refactor agent for Flood-RM.

Owned files:
- `src/sfincs_runs/**`
- tests related specifically to `src/sfincs_runs/**`
- `docs/agent_reports/sfincs_runs.md`

Do not touch:
- `src/power/**`
- `src/design_events/**`
- `src/wflow_runs/**`
- `src/fiat_runs/**`
- shared files outside `src/sfincs_runs/**`, except TODO notes in your agent report
- notebooks, unless you only document the required future import migration

Read first:
- `AGENTS.md`
- `docs/plumbing_reduction_directive.md`
- `docs/codebase_map.md`
- `docs/notebook_backend_trace.md`
- `docs/refactor_roadmap.md`
- `docs/reduction_candidates.md`
- `docs/grill_review.md` if present
- `docs/agent_work_packages.md`
- `src/aoi.py`, `src/paths.py`, and `src/study_location.py` as production syntax examples

Current cleanup state:
- `src/sfincs_runs/notebook.py` has been deleted; notebooks should use `sfincs_runs.config.load_sfincs_runtime`.
- `src/sfincs_runs/scenarios/io.py` has been deleted; `parse_sfincs_inp` lives in `sfincs_runs.config`, generic JSON writes are direct, and event-only SFINCS file writers are private to `scenarios.event_forcing`.
- `src/sfincs_runs/build_base/region_setup.py` has been deleted; `RegionSetup` and `build_region_setup` live in `build_base.static_intake`.
- `src/sfincs_runs/single_use_case/` has been deleted; example-event planning is private to `scenarios.event_forcing.build_event`.
- Do not recreate those facades.

Goal:
Continue the SFINCS deletion bridge while preserving scientific behavior: SFINCS base model setup, SnapWave setup, hydrology/infiltration, scenario forcing, diagnostics, tide gauges, static intake, Hydrodynamic Truth Set semantics, Wave-Coupled Truth Set semantics, and notebook-facing imports.

Syntax standard:
- Prefer the style now used in `src/aoi.py`, `src/paths.py`, and `src/study_location.py`: plain functions, `Path`, direct `geopandas`/`shapely`/`xarray`/`rasterio`/`pandas`/`numpy`, direct `json.dumps`/`yaml.safe_dump`, and minimal dataclasses only where a returned plan object earns its keep.
- Use HydroMT/HydroMT-SFINCS native functions where they already express the model setup; keep only thin compatibility code for Flood-RM artifact paths or unsupported SFINCS syntax.
- Do not move SFINCS concepts into `paths.py`, `aoi.py`, or `study_location.py`. Those files are for repo/location/AOI basics only.

Deletion bridge:
1. Run `rg` call-site checks before deleting or moving anything.
2. Classify each candidate as `CORE_SCIENCE`, `NOTEBOOK_API`, `PLUMBING_IO`, `VALIDATION_QA`, `PLOTTING_REPORTING`, `REDUNDANT_OR_REPLACEABLE`, or `STALE_OR_DANGEROUS`.
3. Delete or inline only shallow modules that fail the deletion test.
4. Keep SFINCS file-format semantics local to the module that writes or reads the file.
5. Prefer direct HydroMT-SFINCS calls over redefined setup logic when equivalence is clear.
6. Mark uncertain competing implementations as "needs evidence"; do not delete them.

High-value targets:
- `build_base/region_notebook.py`: reduce notebook orchestration and static-intake plotting; do not re-split into shallow pass-through files.
- `build_base/inland_base.py`: split only by coherent responsibility if needed: domain planning, Wflow-native river handoff placement, HydroMT build execution, plotting/QA. Preserve handoff geometry semantics.
- `scenarios/event_forcing.py`, `run_events.py`, `run_inland_coupled_events.py`, `scenarios/scenarios.py`, and `scenarios/audit.py`: remove generic path/JSON/CLI plumbing; keep forcing-window, manifest, timing, and SFINCS input semantics explicit.
- `diagnostics.py`: separate plotting/stat products only where it reduces line count and keeps notebook-facing names stable.

Line-reduction expectations:
- Measure before/after line counts for touched files with `wc -l`.
- Prefer deleting facades, inlining one-use helpers, and using native scientific-library calls over creating more private modules.
- A good pass should remove shallow code or materially shrink an oversized file; if a file remains large because it owns real SFINCS behavior, document why.

Do not change without explicit evidence and tests:
- SFINCS input-file keys, timing fields, water-level forcing, precipitation forcing, wave forcing, boundary/source files, coordinate handling, infiltration semantics, subgrid outputs, scenario folder structure, or Wflow-SFINCS handoff geometry.
- Do not run SFINCS.
- Do not download large datasets.
- Do not require credentials.

Run:
- `uv run python -m compileall src`
- `uv run python -m pytest tests/sfincs_runs`
- `uv run python -m pytest`

Done when:
- Deleted/reduced helpers are listed with call-site evidence.
- Preserved notebook-facing SFINCS APIs are listed.
- Remaining "needs evidence" candidates are documented.
- `docs/agent_reports/sfincs_runs.md` records changed files, line-count reduction, validation results, and cross-domain TODOs.
