/improve-codebase-architecture

You are the design-events and source-collection refactor agent for Flood-RM.

Owned files:
- `src/design_events/**`
- tests related specifically to `src/design_events/**`
- `docs/agent_reports/design_events.md`

Do not touch:
- `src/power/**`
- `src/sfincs_runs/**`
- `src/wflow_runs/**`
- `src/fiat_runs/**`
- shared files outside `src/design_events/**`, except TODO notes in your agent report
- notebooks, unless you only document the required future import migration

Read first:
- `AGENTS.md`
- `docs/plumbing_reduction_directive.md`
- `docs/codebase_map.md`
- `docs/notebook_backend_trace.md`
- `docs/refactor_roadmap.md`
- `docs/reduction_candidates.md`
- `docs/grill_review.md` if present
- `docs/agent_work_packages.md`
- `src/aoi.py`, `src/paths.py`, and `src/study_location.py` as production syntax examples

Goal:
Reduce `src/design_events/**` toward direct scientific Python: small modules, explicit source adapters, direct `pandas`/`numpy`/`scipy`/`xarray`/`pathlib`/`json`/`yaml` calls, and no generic helper files that only hide ordinary library behavior. Preserve Event Catalog semantics, event-selection semantics, historical fitting semantics, stochastic member semantics, notebook-facing imports, artifact paths, and side effects.

Syntax standard:
- Prefer the style now used in `src/aoi.py`, `src/paths.py`, and `src/study_location.py`: short functions, direct imports, direct library calls, plain `Path`, plain `json.dumps`/`yaml.safe_dump`, and readable data operations.
- Do not create framework-like adapters, registries, base classes, or catchall utility modules.
- Keep only helpers that encode a named Flood-RM concept or remove real repeated complexity.

Deletion bridge:
1. Run `rg` call-site checks before deleting or moving anything.
2. Classify each candidate as `CORE_SCIENCE`, `NOTEBOOK_API`, `PLUMBING_IO`, `VALIDATION_QA`, `PLOTTING_REPORTING`, `REDUNDANT_OR_REPLACEABLE`, or `STALE_OR_DANGEROUS`.
3. Delete, inline, or relocate only `REDUNDANT_OR_REPLACEABLE` helpers with evidence.
4. Move source-collection adapter behavior into the adapter module that owns the source, not into a shared utility catchall.
5. Replace generic JSON/path/manifest/scalar helpers with direct code at the owning call site unless the manifest schema is a domain artifact contract.
6. Record uncertain code as "needs evidence" in `docs/agent_reports/design_events.md`, not as deleted.

High-value targets:
- `collect_sources/workflow.py`: shrink notebook orchestration; keep source adapters local.
- `collect_sources/national_hydrography.py`, `aorc_sst.py`, `nwm.py`, and related adapters: remove repeated path/source-artifact plumbing while preserving provider-specific behavior.
- `build_events/selection.py`, `build_events/coastal.py`, and probability modules: simplify dataframe/list manipulation only where event semantics are unchanged and tested.
- `plotting.py`: prefer direct matplotlib helpers over custom plotting abstractions when wrappers add no domain value.

Line-reduction expectations:
- Measure before/after line counts for touched files with `wc -l`.
- Prefer deletion or inlining over splitting into more files.
- A good pass should remove shallow code or make one oversized file materially smaller; if deletion is unsafe, document the exact blocker and the characterization test needed.

Do not change without explicit evidence and tests:
- tail sampling
- probability weighting
- event-member construction
- historical analog pairing
- storm timing, rainfall scaling, return-period, or catalog-selection semantics
- artifact file formats consumed by SFINCS or Wflow

Run:
- `uv run python -m compileall src`
- `uv run python -m pytest tests/design_events`
- `uv run python -m pytest`

Done when:
- Deleted or reduced helpers are listed with call-site evidence.
- Preserved Event Catalog and notebook-facing APIs are listed.
- Remaining "needs evidence" candidates are documented.
- `docs/agent_reports/design_events.md` records changed files, line-count reduction, validation results, and cross-domain TODOs.
